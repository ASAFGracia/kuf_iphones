# ✅ Чек-лист перед коммитом

## 🔒 Безопасность

- [ ] Проверьте что `.env` не попадет в коммит:
  ```bash
  git status | grep .env
  # Не должно быть результатов
  ```

- [ ] Проверьте что нет реальных токенов в коде:
  ```bash
  grep -r "8499560253\|8327994324" . --exclude-dir=.git --exclude-dir=logs --exclude-dir=kuf_iphones
  # Не должно быть результатов
  ```

- [ ] Проверьте что нет реальных паролей в коде:
  ```bash
  grep -r "parser_password_2024" . --exclude-dir=.git --exclude-dir=logs --exclude-dir=kuf_iphones
  # Не должно быть результатов
  ```

- [ ] Проверьте что логи не попадут в коммит:
  ```bash
  git status | grep logs
  # Не должно быть результатов
  ```

## 📝 Файлы для коммита

- [x] `.env.example` - шаблон настроек
- [x] `.gitignore` - правильно настроен
- [x] `README.md` - обновлен без секретов
- [x] `SETUP_GITHUB.md` - инструкция для нового компьютера
- [x] `GIT_SETUP.md` - инструкция по коммиту
- [x] Все исходные файлы проекта

## ❌ Файлы НЕ для коммита

- [x] `.env` - реальные токены и пароли
- [x] `logs/` - логи приложения
- [x] `kuf_iphones/` - старый проект
- [x] `__pycache__/` - Python кэш
- [x] `*.pyc` - скомпилированные файлы

## 🚀 Команды для коммита

```bash
# 1. Проверка статуса
git status

# 2. Добавление файлов
git add .

# 3. Проверка что будет закоммичено
git status

# 4. Создание коммита
git commit -m "Initial commit: Parser for Avito and Kufar with Telegram bots"

# 5. Отправка в GitHub
git push -u origin main
```

## ⚠️ ВАЖНО

Если вы случайно закоммитили `.env` или другие секреты:

1. **Немедленно смените все токены и пароли!**
2. Удалите файл из истории Git (требует force push)
3. Или создайте новый репозиторий

**Лучше перестраховаться и проверить все перед коммитом!**

