# 📦 Установка проекта

## 🚀 Быстрая установка (после клонирования)

### 1. Клонирование репозитория

```bash
git clone https://github.com/ASAFGracia/kuf_iphones.git
cd kuf_iphones
```

### 2. Создание .env файла

```bash
cp .env.example .env
```

### 3. Настройка .env

Откройте `.env` в любом текстовом редакторе и заполните:

**Telegram Bot Tokens:**
1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/newbot` и следуйте инструкциям
4. Скопируйте токен в `.env`

**Telegram User ID (для админа):**
1. Найдите @userinfobot в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID в `ADMIN_USER_ID`

**Пароль базы данных:**
- Придумайте надежный пароль
- Запишите его в `DB_PASSWORD`

### 4. Запуск проекта

```bash
# Проверка что Docker запущен
docker ps

# Запуск проекта
docker-compose up -d

# Просмотр логов (для проверки)
docker-compose logs -f app
```

### 5. Проверка работы

1. Откройте Telegram
2. Найдите ботов:
   - @iphones_avito_bot
   - @iphones_kufar_bot
3. Отправьте `/start` любому боту
4. Бот должен ответить и запросить никнейм

## ✅ Готово!

Проект установлен и запущен. Теперь можно:
- Настроить город и модель через бота
- Просматривать логи: `docker-compose logs -f app`
- Подключиться к БД (см. README.md)

## 📚 Дополнительно

- `QUICK_START.md` - быстрый старт и перезапуск
- `README.md` - полная документация
- `COMMANDS.md` - команды ботов

