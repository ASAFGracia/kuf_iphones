# 🚀 Быстрый старт

## Запуск одной командой

```bash
docker-compose up -d
```

Готово! Проект запущен.

## Проверка работы

```bash
# Просмотр логов
docker-compose logs -f app

# Проверка статуса контейнеров
docker-compose ps
```

## Остановка

```bash
docker-compose down
```

## Подключение к базе данных

### Через DBeaver:

```
Тип: PostgreSQL
Хост: localhost
Порт: 5432
База: parser_db
Пользователь: parser_user
Пароль: your_secure_password_here
```

### Через командную строку:

```bash
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db
```

## Использование ботов

1. Найдите в Telegram:
   - @iphones_avito_bot
   - @iphones_kufar_bot

2. Отправьте `/start`

3. Настройте:
   - `/city` - выберите город
   - `/model` - выберите модель
   - `/price` - установите максимальную цену

4. Готово! Бот начнет искать выгодные предложения автоматически.

## Логи

Все логи находятся в `logs/`:
- `main.log` - основной лог
- `avito_parser.log` - парсер Avito
- `kufar_parser.log` - парсер Kufar
- `avito_bot.log` - бот Avito
- `kufar_bot.log` - бот Kufar
- И другие...

## Проблемы?

См. раздел "Устранение проблем" в README.md

