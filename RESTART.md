# 🔄 Перезапуск проекта

## Вариант 1: Docker (рекомендуется)

### Полный перезапуск (с пересборкой):

```bash
# Остановить контейнеры
docker-compose down

# Пересобрать образ (если были изменения в коде)
docker-compose build --no-cache app

# Запустить заново
docker-compose up -d

# Проверить логи
docker-compose logs -f app
```

### Быстрый перезапуск (без пересборки):

```bash
# Перезапустить контейнеры
docker-compose restart app

# Или остановить и запустить
docker-compose stop app
docker-compose start app

# Проверить логи
docker-compose logs -f app
```

### Если нужно применить изменения в коде:

```bash
# Остановить
docker-compose down

# Пересобрать образ
docker-compose build app

# Запустить
docker-compose up -d

# Проверить
docker-compose logs -f app
```

## Вариант 2: Локальный запуск

### Остановка:

Нажмите `Ctrl+C` в терминале, где запущен проект

### Запуск:

```bash
# Активируйте виртуальное окружение (если используете)
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Запустите проект
python main.py
```

## Проверка после перезапуска

### 1. Проверка статуса контейнеров:

```bash
docker-compose ps
```

Должно быть:
```
NAME                    STATUS
avito_kufar_app         Up (healthy)
avito_kufar_postgres    Up (healthy)
```

### 2. Проверка логов:

```bash
# Все логи
docker-compose logs -f app

# Только парсеры
docker-compose logs -f app | grep -i "parser\|найдено"

# Только ошибки
docker-compose logs -f app | grep -i "error\|ошибка"
```

### 3. Проверка работы ботов:

1. Откройте Telegram
2. Найдите ботов: @iphones_avito_bot и @iphones_kufar_bot
3. Отправьте `/start` - бот должен ответить
4. Отправьте `/status` - должны показаться настройки

## Частые проблемы при перезапуске

### Проблема: "Container keeps restarting"

**Решение:**
```bash
# Посмотрите логи ошибок
docker-compose logs app

# Пересоберите образ
docker-compose build --no-cache app
docker-compose up -d
```

### Проблема: Изменения не применились

**Решение:**
```bash
# Убедитесь, что пересобрали образ
docker-compose build --no-cache app

# Или пересоздайте контейнер
docker-compose down
docker-compose up -d --build
```

### Проблема: База данных не работает

**Решение:**
```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Перезапустите только БД
docker-compose restart postgres

# Проверьте логи
docker-compose logs postgres
```

## Полезные команды

```bash
# Просмотр всех контейнеров
docker ps

# Просмотр логов в реальном времени
docker-compose logs -f app

# Остановка всех контейнеров
docker-compose down

# Остановка с удалением данных
docker-compose down -v

# Пересборка без кэша
docker-compose build --no-cache app

# Просмотр использования ресурсов
docker stats

# Очистка неиспользуемых ресурсов
docker system prune -a
```

