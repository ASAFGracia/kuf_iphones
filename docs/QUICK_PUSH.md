# 🚀 Быстрый push в GitHub

## Автоматический способ (рекомендуется)

```bash
./PUSH_TO_GITHUB.sh
```

Скрипт автоматически:
1. Проверит безопасность
2. Инициализирует Git (если нужно)
3. Настроит remote репозиторий
4. Добавит файлы
5. Создаст коммит
6. Отправит в GitHub

## Ручной способ

### 1. Инициализация Git (если нужно)
```bash
git init
```

### 2. Добавление remote репозитория
```bash
git remote add origin https://github.com/ASAFGracia/kuf_iphones.git
```

Если remote уже существует:
```bash
git remote set-url origin https://github.com/ASAFGracia/kuf_iphones.git
```

### 3. Проверка безопасности
```bash
./CHECK_BEFORE_COMMIT.sh
```

### 4. Добавление файлов
```bash
git add .
```

### 5. Проверка что будет закоммичено
```bash
git status
```

**ВАЖНО:** Убедитесь что `.env` НЕ в списке!

### 6. Создание коммита
```bash
git commit -m "Initial commit: Parser for Avito and Kufar with Telegram bots"
```

### 7. Отправка в GitHub

**Если репозиторий пустой:**
```bash
git push -u origin main
```

**Если в репозитории уже есть код (старый проект):**

Вариант 1: Перезаписать (ОСТОРОЖНО!)
```bash
git push -u origin main --force
```

Вариант 2: Работать в отдельной ветке
```bash
git checkout -b new-parser
git push -u origin new-parser
```

Вариант 3: Слить с существующим кодом
```bash
git pull origin main --allow-unrelated-histories
# Разрешите конфликты если нужно
git push -u origin main
```

## ⚠️ Важно

Если в репозитории уже есть старый проект `kuf_iphones`:
- Рекомендуется работать в отдельной ветке
- Или переименовать старую ветку перед push
- Или использовать force push (только если уверены!)

## 🔍 Проверка после push

```bash
# Проверка remote
git remote -v

# Проверка статуса
git status

# Просмотр последних коммитов
git log --oneline -5
```

## 📝 Если возникли проблемы

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ASAFGracia/kuf_iphones.git
```

### Ошибка: "failed to push some refs"
```bash
# Если в репозитории уже есть код
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Ошибка: "authentication failed"
```bash
# Используйте Personal Access Token вместо пароля
# Или настройте SSH ключи
```
