# 📦 Подготовка к коммиту в GitHub

## ✅ Что уже сделано

1. ✅ `.gitignore` настроен - исключает:
   - `.env` файлы (токены и пароли)
   - Логи (`logs/`, `*.log`)
   - Старый проект (`kuf_iphones/`)
   - Python кэш и виртуальные окружения
   - Docker volumes

2. ✅ Создан `.env.example` - шаблон для настройки

3. ✅ Удалены реальные токены и пароли из документации
   - Все заменено на плейсхолдеры

4. ✅ `docker-compose.yml` использует переменные окружения

5. ✅ Создан `SETUP_GITHUB.md` - инструкция для нового компьютера

## 🚀 Команды для коммита

### Первая настройка (если Git еще не инициализирован)

```bash
# Инициализация репозитория
git init

# Добавление удаленного репозитория
git remote add origin <your-github-repo-url>

# Проверка что .env не попадет в коммит
git status
# .env НЕ должен быть в списке
```

### Проверка перед коммитом

```bash
# Проверка что чувствительные данные не попадут в коммит
git status

# Проверка что .env игнорируется
git check-ignore .env
# Должно вывести: .env

# Проверка что логи игнорируются
git check-ignore logs/
# Должно вывести: logs/
```

### Коммит и пуш

```bash
# Добавить все файлы (кроме игнорируемых)
git add .

# Проверить что будет закоммичено
git status

# Создать коммит
git commit -m "Initial commit: Parser for Avito and Kufar with Telegram bots"

# Отправить в GitHub
git push -u origin main
# или
git push -u origin master
```

## 🔒 Проверка безопасности

### Перед коммитом проверьте:

1. **Нет ли реальных токенов в коде:**
   ```bash
   grep -r "8499560253\|8327994324" . --exclude-dir=.git
   # Не должно быть результатов
   ```

2. **Нет ли паролей в коде:**
   ```bash
   grep -r "parser_password_2024" . --exclude-dir=.git
   # Не должно быть результатов
   ```

3. **Файл .env не попадет в коммит:**
   ```bash
   git status | grep .env
   # Не должно быть результатов
   ```

## 📝 Рекомендуемая структура коммитов

```bash
# Первый коммит
git commit -m "Initial commit: Parser for Avito and Kufar"

# Дальнейшие коммиты
git commit -m "Add: New feature description"
git commit -m "Fix: Bug description"
git commit -m "Update: Documentation"
```

## 🎯 Что будет в репозитории

✅ **Будет закоммичено:**
- Весь исходный код
- Конфигурационные файлы (без секретов)
- Документация
- Docker файлы
- `.env.example` (шаблон)

❌ **НЕ будет закоммичено:**
- `.env` (реальные токены и пароли)
- Логи
- Старый проект `kuf_iphones/`
- Python кэш
- Docker volumes

## 🔄 После клонирования на новом компьютере

1. Скопировать `.env.example` в `.env`
2. Заполнить `.env` своими данными
3. Запустить `docker-compose up -d`

См. подробную инструкцию в `SETUP_GITHUB.md`

