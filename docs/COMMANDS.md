# 📋 Список команд ботов

## 🔵 Команды для всех пользователей

### Основные команды

- `/start` - Начать работу с ботом
  - При первом запуске запросит никнейм для аналитики
  - Показывает текущие настройки и доступные команды

- `/help` - Показать справку по командам
  - Выводит список всех доступных команд

- `/profile` - Просмотр своего профиля
  - Показывает:
    - ID пользователя
    - Никнейм и username
    - Настройки (город, модель, максимальная цена)
    - Статистику (выслано объявлений, действий в боте, нажатий кнопок)
    - Права доступа (админ или нет)

### Настройки парсинга

- `/city` - Выбрать город для парсинга
  - Показывает список доступных городов
  - Для Avito: Москва, Санкт-Петербург и др.
  - Для Kufar: Минск, Витебск

- `/model` - Выбрать модель iPhone
  - Показывает список всех доступных моделей
  - Можно выбрать конкретную модель или "Все модели"
  - Поддерживаются модели от iPhone X до iPhone 17

- `/price` - Установить максимальную цену
  - Введите максимальную цену в рублях (Avito) или BYN (Kufar)
  - Отправьте 0 чтобы убрать ограничение
  - Бот будет присылать только объявления ниже указанной цены

- `/status` - Показать текущие настройки
  - Выводит все текущие настройки парсинга
  - Показывает статус (активен/на паузе)

### Управление парсингом

- `/pause` - Поставить парсинг на паузу
  - Останавливает поиск новых объявлений
  - Настройки сохраняются

- `/resume` - Возобновить парсинг
  - Продолжает поиск объявлений с сохраненными настройками

## 🔧 Команды для администраторов

**Доступ:** Только для пользователей с `is_admin = TRUE` в базе данных

### SQL запросы

- `/sql` - Активировать интерактивный режим SQL
  - После команды можно вводить SQL запросы напрямую
  - Разрешены только SELECT запросы
  - Автоматически добавляется LIMIT 100 (если не указан)
  - Максимум 50 строк в ответе
  
  **Примеры:**
  ```
  /sql
  SELECT * FROM users LIMIT 10
  SELECT COUNT(*) FROM advertisements WHERE source = 'kufar'
  SELECT * FROM user_logs ORDER BY created_at DESC LIMIT 20
  ```

- `/stopsql` - Выйти из режима SQL
  - Отключает интерактивный режим SQL
  - После этого обычные команды снова работают

### Аналитика

- `/analytics` - Показать аналитику проекта
  - Количество участников (всего, активных, по источникам)
  - Количество объявлений (всего, по источникам, отправленных)
  - Топ пользователей по действиям
  - Статистика по моделям iPhone

## 📝 Примеры использования

### Настройка бота для поиска iPhone 13 в Санкт-Петербурге (Avito)

```
/start
/city → Выбрать "Санкт-Петербург"
/model → Выбрать "iPhone 13"
/price → Ввести "50000"
/resume
```

### Настройка бота для поиска iPhone 15 в Минске (Kufar)

```
/start
/city → Выбрать "Минск"
/model → Выбрать "iPhone 15"
/price → Ввести "2000"
/resume
```

### Просмотр профиля

```
/profile
```

### Просмотр аналитики (только для админа)

```
/analytics
```

### Выполнение SQL запроса (только для админа)

```
/sql
SELECT * FROM users WHERE is_active = TRUE
SELECT user_id, nickname, city, model FROM users
/stopsql
```

## 🔐 Как дать пользователю права админа

### Через SQL запрос (для текущего админа)

```sql
/sql
UPDATE users SET is_admin = TRUE WHERE user_id = <ID_ПОЛЬЗОВАТЕЛЯ>
```

### Через DBeaver или другой SQL клиент

```sql
UPDATE users SET is_admin = TRUE WHERE user_id = <ID_ПОЛЬЗОВАТЕЛЯ>;
```

**Где взять ID пользователя:**
- Пользователь может узнать свой ID через команду `/profile`
- Или через SQL: `SELECT user_id, username, nickname FROM users`

## 📊 Условия отправки объявлений

Бот отправляет объявление только если:

1. **Цена ниже медианной на 15% ИЛИ на фиксированную сумму:**
   - Для Kufar (Беларусь): дешевле на 15% ИЛИ на 200 BYN
   - Для Avito (Россия): дешевле на 15% ИЛИ на 6000 RUB

2. **Цена не превышает установленный максимум** (если установлен)

3. **Модель соответствует выбранной** (если выбрана конкретная модель)

4. **Объявление еще не было отправлено** (проверка по `notified`)

## ⚙️ Настройки парсинга

- **Интервал парсинга:** 2 минуты (настраивается в `config.py`)
- **Количество страниц:** 10 последних страниц (настраивается в `config.py`)
- **Медианная цена:** Пересчитывается ежедневно в 3:00 ночи

## 🔄 Перезапуск бота

Если нужно перезапустить бота (например, после изменений):

```bash
docker-compose restart app
```

Или полный перезапуск:

```bash
docker-compose down
docker-compose up -d
```

## 📱 Боты

- **Avito бот:** @iphones_avito_bot
- **Kufar бот:** @iphones_kufar_bot

Оба бота работают независимо - можно настроить разные города и модели для каждого.

