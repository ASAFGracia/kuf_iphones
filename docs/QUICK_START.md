# 🚀 Быстрый старт проекта

## 📋 Вариант 1: Первая установка (после клонирования из GitHub)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/ASAFGracia/kuf_iphones.git
cd kuf_iphones
```

### Шаг 2: Создание файла .env

```bash
cp .env.example .env
```

### Шаг 3: Заполнение .env

Откройте `.env` и заполните своими данными:

```env
# Telegram Bot Tokens (получите у @BotFather)
TELEGRAM_AVITO_BOT_TOKEN=your_avito_bot_token_here
TELEGRAM_KUFAR_BOT_TOKEN=your_kufar_bot_token_here

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=parser_db
DB_USER=parser_user
DB_PASSWORD=your_secure_password_here

# Parser Settings
PARSING_INTERVAL_MINUTES=2
PARSING_PAGES_COUNT=10

# Admin Settings (ваш Telegram User ID - узнайте через @userinfobot)
ADMIN_USER_ID=your_telegram_user_id_here
```

### Шаг 4: Запуск проекта

```bash
# Убедитесь что Docker запущен
docker ps

# Запуск проекта
docker-compose up -d

# Проверка логов
docker-compose logs -f app
```

### Шаг 5: Проверка работы

1. Откройте Telegram
2. Найдите ботов: @iphones_avito_bot и @iphones_kufar_bot
3. Отправьте `/start` любому боту
4. Бот должен ответить и запросить никнейм

## 📋 Вариант 2: Перезапуск проекта (когда уже установлен)

### Быстрый перезапуск (без пересоздания контейнеров)

```bash
# Остановка
docker-compose stop

# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f app
```

### Полный перезапуск (с пересозданием контейнеров)

```bash
# Остановка и удаление контейнеров
docker-compose down

# Запуск заново
docker-compose up -d

# Просмотр логов
docker-compose logs -f app
```

### Перезапуск с обновлением кода (после git pull)

```bash
# 1. Получить обновления
git pull origin master

# 2. Пересобрать образ (если изменился Dockerfile или зависимости)
docker-compose build

# 3. Перезапустить
docker-compose down
docker-compose up -d

# 4. Проверить логи
docker-compose logs -f app
```

### Полная переустановка (с удалением всех данных)

⚠️ **ВНИМАНИЕ:** Это удалит все данные из базы данных!

```bash
# Остановка и удаление всего (включая volumes)
docker-compose down -v

# Удаление образов (опционально)
docker-compose rm -f

# Запуск заново
docker-compose up -d

# Проверка
docker-compose logs -f app
```

## 🔧 Полезные команды

### Просмотр статуса

```bash
# Статус контейнеров
docker-compose ps

# Логи приложения
docker-compose logs -f app

# Логи базы данных
docker-compose logs -f postgres
```

### Остановка проекта

```bash
# Остановка (контейнеры остаются)
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением данных
docker-compose down -v
```

### Перезапуск конкретного сервиса

```bash
# Перезапуск только приложения
docker-compose restart app

# Перезапуск только базы данных
docker-compose restart postgres
```

### Подключение к базе данных

```bash
# Через psql
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db

# Через DBeaver (см. README.md)
```

## 🐛 Решение проблем

### Проблема: "Cannot connect to Docker daemon"

```bash
# Убедитесь что Docker Desktop запущен
# macOS/Windows: Откройте Docker Desktop
# Linux: sudo systemctl start docker
```

### Проблема: "Port 5432 is already allocated"

```bash
# Остановите локальный PostgreSQL
# macOS: brew services stop postgresql@15
# Linux: sudo systemctl stop postgresql

# Или измените порт в docker-compose.yml
```

### Проблема: Контейнеры не запускаются

```bash
# Просмотр детальных логов
docker-compose logs app

# Пересоздание контейнеров
docker-compose down
docker-compose up -d

# Если не помогло - полная переустановка
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Изменения в коде не применяются

```bash
# Пересобрать образ
docker-compose build

# Перезапустить
docker-compose down
docker-compose up -d
```

## 📝 Чек-лист первой установки

- [ ] Docker установлен и запущен
- [ ] Репозиторий склонирован
- [ ] Файл `.env` создан из `.env.example`
- [ ] Все токены и пароли заполнены в `.env`
- [ ] `docker-compose up -d` выполнен успешно
- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] Боты отвечают в Telegram

## 📚 Дополнительная документация

- `README.md` - полная документация
- `SETUP_GITHUB.md` - детальная инструкция для нового компьютера
- `COMMANDS.md` - список команд ботов
- `RESTART.md` - инструкции по перезапуску
