# 🚀 Настройка проекта на новом компьютере

Этот файл содержит инструкции по настройке проекта после клонирования из GitHub.

## 📋 Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd PARSER_Avito
```

## 📋 Шаг 2: Создание файла .env

Скопируйте шаблон и заполните своими данными:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

```env
# Telegram Bot Tokens
# Получите токены у @BotFather в Telegram
TELEGRAM_AVITO_BOT_TOKEN=your_avito_bot_token_here
TELEGRAM_KUFAR_BOT_TOKEN=your_kufar_bot_token_here

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=parser_db
DB_USER=parser_user
DB_PASSWORD=your_secure_password_here

# Parser Settings
PARSING_INTERVAL_MINUTES=2
PARSING_PAGES_COUNT=10

# Admin Settings
ADMIN_USER_ID=your_telegram_user_id_here
```

## 📋 Шаг 3: Получение токенов ботов

1. Откройте Telegram
2. Найдите бота @BotFather
3. Отправьте `/newbot` и следуйте инструкциям
4. Скопируйте полученный токен в `.env`

**Или используйте существующие боты:**
- @iphones_avito_bot
- @iphones_kufar_bot

## 📋 Шаг 4: Узнать свой Telegram User ID

1. Найдите бота @userinfobot в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID в `.env` (поле `ADMIN_USER_ID`)

## 📋 Шаг 5: Запуск проекта

```bash
# Убедитесь что Docker запущен
docker ps

# Запустите проект
docker-compose up -d

# Проверьте логи
docker-compose logs -f app
```

## 📋 Шаг 6: Проверка работы

1. Откройте Telegram
2. Найдите ваших ботов
3. Отправьте `/start`
4. Бот должен ответить и запросить никнейм

## 🔒 Безопасность

**ВАЖНО:**
- Файл `.env` НЕ должен попадать в Git (уже в `.gitignore`)
- Никогда не коммитьте токены и пароли
- Используйте разные пароли для разных окружений

## 📝 Дополнительная настройка

### Изменение пароля БД

Если хотите изменить пароль БД:

1. Обновите `DB_PASSWORD` в `.env`
2. Обновите `POSTGRES_PASSWORD` в `docker-compose.yml` (или используйте переменную окружения)
3. Пересоздайте контейнеры:
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### Настройка интервала парсинга

Измените `PARSING_INTERVAL_MINUTES` в `.env`:
- `1` - каждую минуту
- `2` - каждые 2 минуты (по умолчанию)
- `3` - каждые 3 минуты

### Настройка количества страниц

Измените `PARSING_PAGES_COUNT` в `.env`:
- `5` - последние 5 страниц
- `10` - последние 10 страниц (по умолчанию)
- `20` - последние 20 страниц

## 🆘 Проблемы?

См. раздел "Устранение проблем" в `README.md`

