# Парсер iPhone для Avito и Kufar с Telegram ботами

Автоматический парсер объявлений iPhone с Avito (Россия) и Kufar (Беларусь) с интеграцией Telegram ботов и базой данных PostgreSQL.

## 📋 Содержание

- [Быстрый запуск](#-быстрый-запуск)
- [Настройка на новом компьютере](#-настройка-на-новом-компьютере)
- [Подробная установка](#-подробная-установка)
- [Проверка работы](#-проверка-работы)
- [Подключение к БД](#-подключение-к-базе-данных)
- [Использование ботов](#-использование-ботов)
- [Список команд](#-список-команд)
- [Структура проекта](#️-структура-проекта)
- [Устранение проблем](#-устранение-проблем)

## 🚀 Быстрый запуск

### Шаг 1: Проверка Docker

**ВАЖНО:** Перед запуском убедитесь, что Docker Desktop установлен и запущен!

```bash
# Проверка установки Docker
docker --version

# Проверка запуска Docker
docker ps
```

**Если Docker не установлен:**
- **macOS:** Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop/)
- **Windows:** Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
- **Linux:** Установите через пакетный менеджер вашего дистрибутива

**Если Docker не запущен:**
- **macOS/Windows:** Откройте приложение Docker Desktop и дождитесь полной загрузки (иконка в трее должна быть зеленая)
- **Linux:** Запустите: `sudo systemctl start docker`

### Шаг 2: Проверка файла .env

Убедитесь, что файл `.env` существует и содержит правильные токены:

```bash
# Проверка наличия файла
ls -la .env

# Просмотр содержимого (токены должны быть заполнены)
cat .env
```

Создайте файл `.env` из шаблона:
```bash
cp .env.example .env
```

Затем заполните `.env` своими данными:
```env
TELEGRAM_AVITO_BOT_TOKEN=your_avito_bot_token_here
TELEGRAM_KUFAR_BOT_TOKEN=your_kufar_bot_token_here
DB_HOST=postgres
DB_PORT=5432
DB_NAME=parser_db
DB_USER=parser_user
DB_PASSWORD=your_secure_password_here
PARSING_INTERVAL_MINUTES=2
```

### Шаг 3: Запуск проекта

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса (все контейнеры должны быть "Up")
docker-compose ps
```

### Шаг 4: Проверка логов

```bash
# Просмотр логов приложения
docker-compose logs -f app

# Просмотр логов базы данных
docker-compose logs -f postgres
```

Если видите сообщения об успешной инициализации - всё работает! ✅

## 🖥️ Настройка на новом компьютере

После клонирования репозитория из GitHub:

1. **Создайте файл `.env` из шаблона:**
   ```bash
   cp .env.example .env
   ```

2. **Заполните `.env` своими данными:**
   - Получите токены ботов у @BotFather в Telegram
   - Узнайте свой Telegram User ID через @userinfobot
   - Придумайте надежный пароль для БД

3. **Запустите проект:**
   ```bash
   docker-compose up -d
   ```

**Подробная инструкция:** См. [SETUP_GITHUB.md](SETUP_GITHUB.md)

## 📦 Подробная установка

### Вариант 1: Docker (рекомендуется)

#### Требования:
- Docker Desktop установлен и запущен
- 2+ GB свободной оперативной памяти
- Интернет-соединение

#### Пошаговая установка:

**1. Установка Docker Desktop**

**macOS:**
```bash
# Скачайте с официального сайта
open https://www.docker.com/products/docker-desktop/

# Или через Homebrew
brew install --cask docker
```

**Windows:**
- Скачайте установщик с [официального сайта](https://www.docker.com/products/docker-desktop/)
- Запустите установщик и следуйте инструкциям
- После установки перезагрузите компьютер

**Linux (Ubuntu/Debian):**
```bash
# Обновление пакетов
sudo apt-get update

# Установка зависимостей
sudo apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавление официального GPG ключа
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Установка Docker
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Добавление пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Перезапуск сессии (или перелогин)
newgrp docker
```

**2. Проверка установки Docker**

```bash
# Проверка версии
docker --version
# Должно вывести: Docker version 24.x.x или выше

# Проверка Docker Compose
docker compose version
# Должно вывести: Docker Compose version v2.x.x или выше

# Проверка работы Docker
docker ps
# Должно вывести список контейнеров (может быть пустым, это нормально)
```

**3. Клонирование/подготовка проекта**

```bash
# Перейдите в директорию проекта
cd /Users/rostilin/PARSER_Avito

# Проверьте наличие всех файлов
ls -la
# Должны быть: docker-compose.yml, Dockerfile, main.py, .env и т.д.
```

**4. Настройка переменных окружения**

```bash
# Проверьте файл .env
cat .env

# Если файла нет, создайте его:
cp .env.example .env
# Затем отредактируйте .env и заполните своими данными
```

**5. Запуск проекта**

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Ожидаемый вывод:
# Creating network "parser_avito_parser_network" ... done
# Creating volume "parser_avito_postgres_data" ... done
# Creating avito_kufar_postgres ... done
# Creating avito_kufar_app ... done
```

**6. Проверка статуса**

```bash
# Проверка статуса контейнеров
docker-compose ps

# Ожидаемый вывод:
# NAME                    STATUS          PORTS
# avito_kufar_app         Up (healthy)    ...
# avito_kufar_postgres    Up (healthy)    0.0.0.0:5432->5432/tcp
```

**7. Проверка логов**

```bash
# Логи приложения (должны быть сообщения об успешной инициализации)
docker-compose logs app | tail -20

# Логи базы данных
docker-compose logs postgres | tail -20
```

### Вариант 2: Локальный запуск (без Docker)

#### Требования:
- Python 3.11+
- PostgreSQL 12+ установлен и запущен
- pip установлен

#### Пошаговая установка:

**1. Установка PostgreSQL**

**macOS:**
```bash
# Через Homebrew
brew install postgresql@15
brew services start postgresql@15
```

**Windows:**
- Скачайте установщик с [официального сайта](https://www.postgresql.org/download/windows/)
- Запустите установщик и следуйте инструкциям
- Запомните пароль для пользователя postgres

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install -y postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**2. Создание базы данных**

```bash
# Подключение к PostgreSQL
psql -U postgres

# В консоли PostgreSQL выполните:
CREATE DATABASE parser_db;
CREATE USER parser_user WITH PASSWORD 'your_secure_password_here';
GRANT ALL PRIVILEGES ON DATABASE parser_db TO parser_user;
\q
```

**3. Установка зависимостей Python**

```bash
# Создание виртуального окружения (рекомендуется)
python3 -m venv venv

# Активация виртуального окружения
# macOS/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

**4. Настройка .env для локального запуска**

```bash
# Обновите .env файл
cat > .env << 'EOF'
TELEGRAM_AVITO_BOT_TOKEN=your_avito_bot_token_here
TELEGRAM_KUFAR_BOT_TOKEN=your_kufar_bot_token_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=parser_db
DB_USER=parser_user
DB_PASSWORD=your_secure_password_here
PARSING_INTERVAL_MINUTES=2
EOF
```

**5. Запуск приложения**

```bash
# Создание директории для логов
mkdir -p logs

# Запуск
python main.py
```

## ✅ Проверка работы

### 1. Проверка контейнеров (Docker)

```bash
# Статус всех контейнеров
docker-compose ps

# Должно быть:
# - avito_kufar_postgres: Up (healthy)
# - avito_kufar_app: Up (healthy)
```

### 2. Проверка логов

```bash
# Логи приложения (ищем сообщения об успешной инициализации)
docker-compose logs app | grep -i "запущен\|initialized\|success"

# Должны быть сообщения:
# - "База данных инициализирована успешно"
# - "Бот Avito запущен"
# - "Бот Kufar запущен"
# - "Все сервисы запущены успешно!"
```

### 3. Проверка базы данных

```bash
# Подключение к базе данных
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db

# В консоли PostgreSQL:
\dt  # Показать таблицы
# Должны быть: users, user_logs, advertisements

SELECT COUNT(*) FROM users;  # Проверка таблицы пользователей
\q  # Выход
```

### 4. Проверка ботов в Telegram

1. Откройте Telegram
2. Найдите ботов:
   - @iphones_avito_bot
   - @iphones_kufar_bot
3. Отправьте `/start` любому боту
4. Бот должен ответить приветственным сообщением

## 🔌 Подключение к базе данных

### Через DBeaver

**Параметры подключения:**

```
Тип БД: PostgreSQL
Хост: localhost
Порт: 5432
База данных: parser_db
Пользователь: parser_user
Пароль: your_secure_password_here (из вашего .env файла)
```

**Пошаговая инструкция:**

1. Откройте DBeaver
2. Нажмите "Новое подключение" (иконка вилки) или `Ctrl+Shift+N`
3. Выберите "PostgreSQL"
4. Нажмите "Далее"
5. Заполните параметры:
   - **Хост:** `localhost`
   - **Порт:** `5432`
   - **База данных:** `parser_db`
   - **Пользователь:** `parser_user`
   - **Пароль:** `your_secure_password_here` (из вашего .env файла)
6. Нажмите "Тест подключения"
7. Если тест успешен - нажмите "Готово"
8. Подключение появится в списке слева

### Через командную строку

```bash
# Из контейнера Docker
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db

# Локально (если PostgreSQL установлен локально)
psql -h localhost -U parser_user -d parser_db
```

### Через Python

```python
import psycopg2

conn = psycopg2.connect(
    host="localhost",
    port=5432,
    database="parser_db",
    user="parser_user",
    password="your_secure_password_here"
)

cursor = conn.cursor()
cursor.execute("SELECT COUNT(*) FROM users;")
print(cursor.fetchone())
conn.close()
```

## 📱 Использование ботов

Подробный список всех команд доступен в файле [COMMANDS.md](COMMANDS.md)

### Основные команды

- `/start` - Начать работу (при первом запуске запросит никнейм)
- `/city` - Выбрать город
- `/model` - Выбрать модель iPhone
- `/price` - Установить максимальную цену
- `/status` - Показать текущие настройки
- `/pause` - Поставить парсинг на паузу
- `/resume` - Возобновить парсинг
- `/profile` - Просмотр своего профиля

### Команды для администраторов

- `/sql` - Интерактивный режим SQL запросов (только SELECT)
- `/stopsql` - Выйти из режима SQL
- `/analytics` - Показать аналитику проекта

**Как дать пользователю права админа:**
```sql
UPDATE users SET is_admin = TRUE WHERE user_id = <ID_ПОЛЬЗОВАТЕЛЯ>;
```

## 📋 Список команд

### Боты:
- **@iphones_avito_bot** - для объявлений с Avito (Россия)
- **@iphones_kufar_bot** - для объявлений с Kufar (Беларусь)

### Команды ботов:

- `/start` - Начать работу с ботом
- `/city` - Выбрать город для парсинга
- `/model` - Выбрать модель iPhone
- `/price` - Установить максимальную цену
- `/status` - Показать текущие настройки
- `/pause` - Поставить парсинг на паузу
- `/resume` - Возобновить парсинг
- `/help` - Показать справку

### Настройка:

1. Найдите бота в Telegram
2. Отправьте `/start`
3. Выберите город командой `/city`
4. Выберите модель командой `/model`
5. Установите максимальную цену командой `/price`
6. Бот автоматически начнет искать выгодные предложения!

## 🏗️ Структура проекта

```
PARSER_Avito/
├── main.py                      # 🚀 Главный файл запуска
├── config.py                    # ⚙️ Конфигурация
├── database.py                  # 💾 Работа с БД
│
├── parsers/                     # 📡 ПАРСЕРЫ
│   ├── selectors.py           # ⚙️ КОНФИГУРАЦИЯ СЕЛЕКТОРОВ (настраивать здесь!)
│   ├── avito_parser.py        # Парсер Avito (Россия)
│   └── kufar_parser.py        # Парсер Kufar (Беларусь)
│
├── services/                    # 🔧 Сервисы
│   ├── parser_service.py      # Сервис парсинга
│   └── scheduler.py           # Планировщик задач
│
├── utils/                       # 🛠️ Утилиты
│   ├── logger.py              # Настройка логирования
│   └── median_calculator.py   # Калькулятор медианной цены
│
├── bot_avito.py                 # 🤖 Telegram бот для Avito
├── bot_kufar.py                 # 🤖 Telegram бот для Kufar
│
├── docker-compose.yml           # 🐳 Docker Compose
├── Dockerfile                   # 🐳 Docker образ
├── .env                         # 🔑 Переменные окружения
└── requirements.txt             # 📦 Зависимости
```

**Подробная структура:** См. `PROJECT_STRUCTURE.md`

## 🔧 Устранение проблем

### Проблема: "Cannot connect to the Docker daemon"

**Причина:** Docker Desktop не запущен

**Решение:**
1. Откройте Docker Desktop
2. Дождитесь полной загрузки (иконка в трее должна быть зеленая)
3. Проверьте: `docker ps` (должно работать без ошибок)
4. Попробуйте снова: `docker-compose up -d`

### Проблема: "Port 5432 is already allocated"

**Причина:** Порт 5432 уже занят (возможно, локальный PostgreSQL)

**Решение 1 (остановить локальный PostgreSQL):**
```bash
# macOS
brew services stop postgresql@15

# Linux
sudo systemctl stop postgresql

# Windows: Остановите службу PostgreSQL через Services
```

**Решение 2 (изменить порт в docker-compose.yml):**
```yaml
ports:
  - "5433:5432"  # Измените 5432 на 5433
```

И обновите `.env`:
```env
DB_PORT=5433
```

### Проблема: "Container keeps restarting"

**Причина:** Ошибка в приложении или неправильные настройки

**Решение:**
```bash
# Просмотр детальных логов
docker-compose logs app

# Проверка ошибок
docker-compose logs app | grep -i error

# Пересоздание контейнера
docker-compose down
docker-compose up -d
```

### Проблема: "Database connection failed"

**Причина:** База данных еще не готова или неправильные креды

**Решение:**
```bash
# Проверка статуса PostgreSQL
docker-compose ps postgres

# Проверка логов PostgreSQL
docker-compose logs postgres

# Проверка подключения
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db

# Если не подключается, проверьте .env файл
cat .env
```

### Проблема: "Bot token is invalid"

**Причина:** Неправильный токен бота в `.env`

**Решение:**
1. Проверьте токены в `.env`
2. Убедитесь, что токены скопированы полностью (без пробелов)
3. Перезапустите контейнер:
```bash
docker-compose restart app
```

### Проблема: "No module named 'utils'"

**Причина:** Проблема с импортами в Docker

**Решение:**
```bash
# Пересборка образа
docker-compose build --no-cache app
docker-compose up -d
```

### Проблема: Боты не отвечают

**Причина:** Боты не запущены или ошибка в коде

**Решение:**
```bash
# Проверка логов ботов
docker-compose logs app | grep -i bot

# Проверка, что боты запущены
docker-compose logs app | grep -i "запущен\|started"

# Перезапуск приложения
docker-compose restart app
```

### Проблема: Медианная цена не рассчитывается

**Причина:** Нет данных в базе или ошибка в расчете

**Решение:**
```bash
# Проверка данных в базе
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db -c "SELECT COUNT(*) FROM advertisements;"

# Если данных нет - подождите несколько циклов парсинга (каждые 3 минуты)

# Проверка логов расчета медианы
docker-compose logs app | grep -i median
```

### Проблема: "Permission denied" при запуске Docker

**Причина:** Пользователь не в группе docker (Linux)

**Решение:**
```bash
# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогин или перезапуск сессии
newgrp docker

# Проверка
docker ps
```

### Проблема: Контейнеры не останавливаются

**Решение:**
```bash
# Принудительная остановка
docker-compose down --remove-orphans

# Если не помогает
docker stop $(docker ps -q)
docker-compose down
```

## 📊 Структура базы данных

### Таблица `users`
Хранит информацию о пользователях бота:
- `user_id` - ID пользователя Telegram (PRIMARY KEY)
- `username`, `first_name`, `last_name` - данные пользователя
- `city` - выбранный город
- `model` - выбранная модель iPhone
- `max_price` - максимальная цена
- `source` - источник ('avito' или 'kufar')
- `is_active` - активен ли парсинг для пользователя
- `created_at`, `updated_at` - временные метки

### Таблица `user_logs`
Логи взаимодействия с ботом:
- `id` - ID записи (SERIAL PRIMARY KEY)
- `user_id` - ID пользователя (FOREIGN KEY)
- `action` - действие
- `message_text` - текст сообщения
- `created_at` - время действия

### Таблица `advertisements`
Хранит все найденные объявления:
- `id` - ID записи (SERIAL PRIMARY KEY)
- `avito_id` - ID объявления на Avito
- `kufar_id` - ID объявления на Kufar
- `source` - источник ('avito' или 'kufar')
- `price` - цена
- `model` - модель iPhone
- `city` - город
- `memory` - объем памяти
- `url` - ссылка на объявление
- `median_price` - медианная цена по модели в городе
- `price_difference` - разница между ценой и медианной
- `created_at`, `updated_at` - временные метки

## 🔄 Логика работы

1. **Парсинг:** Каждые 3 минуты парсеры проверяют объявления на Avito и Kufar
2. **Фильтрация:** Для каждого активного пользователя выполняется поиск по его настройкам
3. **Сохранение:** Найденные объявления сохраняются в базу данных
4. **Расчет медианы:** Рассчитывается медианная цена на основе последних 1000 записей за 30 дней
5. **Фильтр выгодности:** Если цена объявления ниже медианной более чем на 15%, оно отправляется пользователю
6. **Ежедневный пересчет:** Каждый день в 3:00 ночи пересчитываются все медианные цены для актуальности
7. **Логирование:** Все действия логируются в файлы и базу данных

## 🏙️ Поддерживаемые города

### Avito (Россия):
Москва, Санкт-Петербург, Новосибирск, Екатеринбург, Казань, Нижний Новгород, Челябинск, Самара, Омск, Ростов-на-Дону, Уфа, Красноярск, Воронеж, Пермь, Волгоград

### Kufar (Беларусь):
Минск, Витебск

## 📱 Поддерживаемые модели iPhone

iPhone 15 Pro Max, iPhone 15 Pro, iPhone 15 Plus, iPhone 15, iPhone 14 Pro Max, iPhone 14 Pro, iPhone 14 Plus, iPhone 14, iPhone 13 Pro Max, iPhone 13 Pro, iPhone 13, iPhone 12 Pro Max, iPhone 12 Pro, iPhone 12, iPhone 11 Pro Max, iPhone 11 Pro, iPhone 11, iPhone XS Max, iPhone XS, iPhone XR, iPhone X, iPhone SE

## 📝 Логирование

Логи сохраняются в директории `logs/`:
- `main.log` - основной лог приложения
- `avito_parser.log` - логи парсера Avito
- `kufar_parser.log` - логи парсера Kufar
- `avito_bot.log` - логи бота Avito
- `kufar_bot.log` - логи бота Kufar
- `database.log` - логи работы с БД
- `parser_service.log` - логи сервиса парсинга
- `scheduler.log` - логи планировщика
- `median_calculator.log` - логи расчета медианной цены

Все логи ротируются при достижении 10 MB (хранится 5 последних файлов).

## ⚙️ Оптимизация медианной цены

- Используются последние **1000 записей** за **30 дней** для расчета
- Баланс между точностью и производительностью
- Ежедневный пересчет в 3:00 ночи для актуальности данных
- Кэширование результатов для быстрого доступа

## 🛠️ Полезные команды Docker

```bash
# Запуск проекта
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка проекта
docker-compose down

# Остановка с удалением данных
docker-compose down -v

# Перезапуск
docker-compose restart app

# Пересборка образа
docker-compose build --no-cache app

# Просмотр статуса
docker-compose ps

# Подключение к базе данных
docker exec -it avito_kufar_postgres psql -U parser_user -d parser_db

# Просмотр использования ресурсов
docker stats

# Очистка неиспользуемых ресурсов
docker system prune -a
```

## 🔧 Настройка парсинга (если не находит объявления)

Если парсер не находит объявления, проблема скорее всего в селекторах.

**Файл для настройки:** `parsers/selectors.py`

Здесь настраиваются селекторы для поиска элементов на страницах Avito и Kufar.

**Подробная инструкция:** См. `PARSING_CONFIG.md`

### Быстрая настройка:

1. Откройте `parsers/selectors.py`
2. Найдите селекторы для нужного сайта (AVITO_SELECTORS или KUFAR_SELECTORS)
3. Обновите селекторы согласно текущей структуре сайта
4. Перезапустите проект: `docker-compose restart app`

### Отладка:

```bash
# Просмотр логов парсера
docker-compose logs -f app | grep -i "parser\|найдено\|ошибка"

# Или локально
tail -f logs/avito_parser.log
tail -f logs/kufar_parser.log
```

В логах будет информация о том, сколько объявлений найдено и какие ошибки возникли.

## 🔧 Настройка парсинга (если не находит объявления)

Если парсер не находит объявления, проблема скорее всего в селекторах.

**Файл для настройки:** `parsers/selectors.py`

Здесь настраиваются селекторы для поиска элементов на страницах Avito и Kufar.

**Подробная инструкция:** См. `PARSING_CONFIG.md`

### Быстрая настройка:

1. Откройте `parsers/selectors.py`
2. Найдите селекторы для нужного сайта (`AVITO_SELECTORS` или `KUFAR_SELECTORS`)
3. Обновите селекторы согласно текущей структуре сайта
4. Перезапустите проект: `docker-compose restart app`

### Отладка:

```bash
# Просмотр логов парсера
docker-compose logs -f app | grep -i "parser\|найдено\|ошибка"

# Или локально
tail -f logs/avito_parser.log
tail -f logs/kufar_parser.log
```

В логах будет информация о том, сколько объявлений найдено и какие ошибки возникли.

## 🔄 Перезапуск проекта

### Быстрый перезапуск (если код не менялся):

```bash
docker-compose restart app
```

### Полный перезапуск (если изменился код):

```bash
# Остановить контейнеры
docker-compose down

# Пересобрать образ
docker-compose build app

# Запустить заново
docker-compose up -d

# Проверить логи
docker-compose logs -f app
```

### Если изменения не применились:

```bash
# Полная пересборка без кэша
docker-compose down
docker-compose build --no-cache app
docker-compose up -d
```

**Подробная инструкция:** См. `RESTART.md`

## 📄 Лицензия

Этот проект создан в образовательных целях. Используйте ответственно и соблюдайте правила использования Avito и Kufar.
